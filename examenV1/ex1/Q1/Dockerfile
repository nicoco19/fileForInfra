# Utiliser l'image officielle PHP avec Apache
FROM php:8.2-apache

# Copier les fichiers PHP dans le répertoire web
COPY . /var/www/html

# Exposer le port 80 pour HTTP
EXPOSE 80

# Apache démarre automatiquement dans cette image

